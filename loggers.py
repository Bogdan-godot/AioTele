import logging

bot = logging.getLogger("botapi.bot")
event = logging.getLogger("botapi.event")
middlewares = logging.getLogger("botapi.middlewares")
webhook = logging.getLogger("botapi.webhook")
scene = logging.getLogger("botapi.scene")